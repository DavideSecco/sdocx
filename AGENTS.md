# Agent context

The shared working context for this repo lives in [`CLAUDE.md`](./CLAUDE.md).
Read it first — it covers the repo map, the format knowledge base
(`docs/format/` + `spec/`), the working discipline, and how to run tests, the
`pysdocx` CLI, and the Kaitai validation.

This file exists so Codex (which reads `AGENTS.md`) and Claude Code (which reads
`CLAUDE.md`) share one source of truth. Keep the content in `CLAUDE.md`; leave
this as a pointer.
